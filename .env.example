# Copy to .env and fill in. main.py doesn't auto-load this yet unless you add
# `from dotenv import load_dotenv; load_dotenv()` at the top — or just set these
# as real environment variables / edit config/settings.py directly.

GCP_PROJECT_ID=your-gcp-project-id
GCP_LOCATION=us-central1
